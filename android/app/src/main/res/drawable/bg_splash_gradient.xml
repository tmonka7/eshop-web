<?xml version="1.0" encoding="utf-8"?>
<!-- The launch screen's field: the same diagonal green the storefront's hero
     and the panel's sign-in screen use, so all three open the same way. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <gradient
        android:type="linear"
        android:angle="135"
        android:startColor="@color/primary"
        android:centerColor="@color/primary_dark"
        android:endColor="@color/primary_ink" />
</shape>
