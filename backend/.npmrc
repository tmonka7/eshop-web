# onnxruntime-node (image search) bundles its CPU binaries. Its postinstall
# would otherwise try to download optional CUDA libraries on Linux x64, which
# fails on an offline host and is not needed.
onnxruntime-node-install=skip
